import { extendTheme } from '@chakra-ui/react';

const colors = {
	primero: '#011F26',
	segundo: '#025E73',
	tercero: '#A5A692',
    cuarto: "#F2A71B",
    fondo: "#F5F5F5"
};

const theme = extendTheme({ colors });

export default theme;